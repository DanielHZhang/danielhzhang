import {Box, Flex} from '@chakra-ui/react';

export const Connect = (): JSX.Element => {
  return (
    <Flex>
      Let's Connect
      <Box>Get in touch for opportunities!</Box>
    </Flex>
  );
};
